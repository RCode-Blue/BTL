import React from "react";

import { SectionHeaderDark } from "../_reusables/contentSection/sectionHeader";

const OurTools = () => {
  const headerText = "Our Tools";
  return <SectionHeaderDark text={headerText} />;
};

export default OurTools;
